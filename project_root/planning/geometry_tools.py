import math

def inflate_obstacle(obstacle, buffer):
    # Given obstacle definition, return a slightly larger obstacle to create a clearance zone
    pass

def distance(a, b):
    return math.sqrt((a[0]-b[0])**2 + (a[1]-b[1])**2)

def path_to_line_points(path):
    # Convert path waypoints into pairs of points for drawing lines
    return path
